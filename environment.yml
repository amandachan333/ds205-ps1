name: food
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.12
  - pip
  - pip:
    # Data collection (Part A: Scraper)
    - selenium>=4.15.0

    # Data manipulation
    - pandas>=2.0.0

    # API development (Part B)
    - fastapi>=0.128.0
    - uvicorn>=0.24.0
    - pydantic>=2.0.0
    - requests>=2.31.0

    # Fuzzy matching (Part B: NOVA enrichment)
    - thefuzz>=0.22.0

    # Testing
    - pytest>=7.4.0
